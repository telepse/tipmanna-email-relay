# Relay authentication (shared with TipManna backend EMAIL_RELAY_API_KEY)
RELAY_API_KEY=

# Qserver SMTP (relay host only — never set on Render)
QSERVER_SMTP_HOST=26.qservers.net
QSERVER_SMTP_PORT=465
QSERVER_SMTP_USER=
QSERVER_SMTP_PASSWORD=
QSERVER_FROM_EMAIL=hello@tipmanna.com
ADMIN_NOTIFICATION_EMAIL=

# HTTP server (Railway sets PORT automatically)
PORT=8080
